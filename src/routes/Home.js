import React from "react";
import Card from "../components/Card";

const Home = ({ cardsValues, setCardsValues, favToggleChange }) => {
  return (
    <div>
      <div className="page-title">home page to be</div>
    </div>
  );
};
export default Home;
